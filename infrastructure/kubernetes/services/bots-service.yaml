---
apiVersion: v1
kind: Service
metadata:
  name: visa-evaluation-bot-service
  namespace: visa-portal
  labels:
    app: visa-evaluation-bot
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: visa-evaluation-bot

---
apiVersion: v1
kind: Service
metadata:
  name: document-preparation-bot-service
  namespace: visa-portal
  labels:
    app: document-preparation-bot
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: document-preparation-bot 